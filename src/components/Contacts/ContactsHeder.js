import React from "react";
import styles from "../Contacts/Contacts.modules.css"

const ContactsHeder = () => (
    <section>
        <h2 className={styles.contactsHeader}>Contacts</h2>
     </section>
     
);

export default ContactsHeder;